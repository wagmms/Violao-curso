{
  "name": "violao-curso",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de estudo e prática de violão com catálogo integrado do Método Tríade",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "mkdir -p dist && cp -r app/* dist/",
    "lint": "node --check server.js app/*.js",
    "test": "node app/ferramentas/validar-piloto.cjs"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
